{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "(npm install) && (cd ../client-admin && npm install && npm run build && npm install bootstrap@5.3.0) && (cd ../client-customer && npm install && npm run build && npm install bootstrap@5.3.0)",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "0.0.1",
    "body": "^5.1.0",
    "crypto": "^1.0.1",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1",
    "nodemailer": "^6.9.13",
    "parser": "^0.1.4"
  }
}